# PIR1 持仓语义符合性纠错预先记录

状态：仅冻结纠错范围和复验门，不包含任何纠错账户结果。基线 main `7b9ed877eb7f14f6b1d93d719a30195674781401`；原 PIR1 协议及拒绝账户保持原状。

已保存 468 次 PIR1 开发账户中，当前子机会与真实持仓身份不同且仍有仓位的调用有 13 次、涉及 9 笔旧仓。当前 `disposition` 对任意子机会先返回 `hold`，绕过 `consumed` 与方向。原协议只允许子机会在空仓时入场，并保留原 SX60 仓位失效退出；子机会均在原事件失效后确认。此前 `test_candidate_signal_does_not_close_an_existing_same_side_campaign` 的断言将已失效旧仓的延续当成正确结果。

最小纠错：持仓有实际成交归属时，只有当前有效机会与所持 campaign 身份相同且方向一致才继续持有。旧父事件失效、子机会当前有效时，旧仓按原计划退出；已成交子仓在本子机会有效期间仍持有；反向机会和不同原事件照原身份规则执行。空仓消费保护、子机会几何与优先级、初始权益、资金预算、调用和执行参数不变。真实仓位归属来自已成交母单身份或恢复状态，不能从市场机会推断。

只做一次 2020—2023、468 调用的纠错 PIR1 连续账户。输入身份固定为 `9b036837f4eb462d672524f9b032adc7a9a9ba85cce071b33b268ab658747645`，与已存同输入 SX60 对照比较；共享源码中的改动仅针对带 `parent_identity` 的机会，对照无此机会，故不重复对照。保存新账户、完整执行和权益原件，不覆盖原 PIR1 失败原件。检查因果、账本、资金、保护和容量；若成本后 CAGR<150%、连续 MDD>=50%、相对配对 SX60 无净优势或由一笔/单一年份主导，纠错版停止晋级。任何新风险分配机制须另行一次冻结，只允许一个新经济候选。

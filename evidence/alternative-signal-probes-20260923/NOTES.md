# 原全窗输入的三个探索性信号探针

这是已多次观察过的 795 调用窗口上的**事后探索**，不是事前冻结的新交易版本，不含成交/仓位/费用/funding/账户权益。检验共享 `coinquant.opportunities.Opportunities` 已有的 `shock`（4h 逆向冲击）、`swing`（日线方向转向）、`impulse_hold`（1h 强冲击），使用原 795 调用、原 Binance BTCUSDT USDT 永续小时原件；缺席调用和执行分钟不参与信号标签。按每个调用前**已完成**的相应小时、四小时或日线价格计算机会，只在评价时取从调用时小时开盘到 168 小时后小时开盘的**事后原始同方向 BTC 涨跌**，不许用标签参与在线模型或称其净收益。聚合按信号调用年和多空分别列调用数、不同机会身份数、标签缺失和原始平均表现；同一机会的重复调用与七日窗口相互重叠。

`reproduce.py` 是只读复现入口，使用原完整小时原件目录（内含 `native/warmup/repairs/RESTORE_RECEIPT.json`）输出全部 3×795 个调用状态。完整逐调用原件 `COINQUANT_ALTERNATIVE_SIGNAL_FULL_CALL_PROBES_20260923.json`：Library ID `libfile_1f8e58a998e481918fe6c8efaaded3b8`、393,673 字节、SHA-256 `8fe17a62ad0dbc38c3e8a25098cad80f50479d159714d75c017c424729e9146f`，上传后回读逐字节一致。输入恢复收据 SHA-256 `547fbc42920823e37561a344d1ff9c9a2d258f5918cada7ae37d5583c34850e9`，市场原件的有序 SHA 聚合 `fb5adb7afeacffdcbce1745111b373ded79f3cc128c4d87bed6d174bc91ad674`；冻结 spec、调用及模型源码哈希也在原件 `source` 中。

| 探针的实际调用信号 | 2022 年同方向 168h 平均原始涨跌 | 2025 年同方向 168h 平均原始涨跌 |
|---|---|---|
| 四小时逆向冲击 | 当年无多头可见调用 | 多头 1 次／1 个身份：+17.56%；样本极少 |
| 日线方向转向 | 多头 42 次／9 个身份：−5.20%；空头 78 次／10 个身份：+0.45% | 多头 55 次／10 个身份：−0.62%；空头 66 次／13 个身份：−0.59% |
| 一小时顺向冲击 | 多头 16 次／9 个身份：−4.45%；空头 14 次／9 个身份：−0.36% | 多头 13 次／7 个身份：−1.80%；空头 16 次／10 个身份：−4.08% |

逆向冲击 2020 多头唯一调用约 −8.87%，2021 有 3 次多头调用、2023/2026 没有多头调用；单凭 2025 的一次正标签不够支持连续账户的回报设想。另两种更广的信号在 2025 多空两侧的原始均值均弱于零，尚未付出费用和 funding；2022 日线多头尤其弱。因此不为这三项探索概念开新成交账户或宣称成本后 CAGR，不把已看过的 2025 当独立样本外。调用数绝不是订单数，原始均值不是独立期望或新交易规则的因果改进。正式目标仍未达成，生产 B36、账户交易阻止不动。

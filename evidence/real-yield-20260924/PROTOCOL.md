# DFII10 覆盖增量：唯一事前实验协议

2026-09-24；此协议在查看新策略的 BTC 收益、账户结果或分钟执行结果前冻结。开发 468 次调用中，ALFRED 原始版本复核有 463 次数据合格、5 次数据过期；这仅是数据覆盖，不是净优势。基线是既有 SX60，正式目标与原资金、795/787 调用、费用、原生保护及执行窗口完全沿用；生产 B36 和 execute 阻止不动。

## 数据与状态

ALFRED DFII10 `new/revised observations only` 的官方历史 vintage ZIP 是唯一宏观数值源。每列日期为 ALFRED 版本日期，行日期为观测日期；不能以行日期代替已知时刻。原始 H.15 发布时刻不被推断为 ALFRED 入库时刻；某特定生产接口的历史可达性仍未知。把每个版本日期的**美东 23:59:59.999**再延迟 **48 小时**，转换到 UTC；仅使用可用时刻**严格早于**原调用的版本。逐版本追加非空观测及修订，在每个调用重建当时完整快照；其余空格表示无新值，不是缺测或零，不允许未来版本回填。周末按最近已知值沿用。最近观测超过 **7 个 UTC 日历日**、历史观测不足、版本不明或数值无效时，禁用宏观分量并记录原因，仍继续整段 SX60 账户。

在同一调用快照中，取最新已知观测和它之前第 **20** 个有数值的观测；仅当最新实际利率 ≤ 该旧值 − **0.25 个百分点**时，下降状态为真。这是一个月左右的持续下降，25 基点抵御单日小幅跳动；它是实际利率下降状态，并非未建预期模型的“意外冲击”。参数均固定，不扫描其他回看、阈值或入库延迟。

## 一份连续账户中的交易规则

在原调用上，优先执行 SX60 原信号及其持仓、保护、资金预算与计划退出；仅当账户空仓、SX60 本次无可执行多头机会、且宏观状态为真时，启动一次额外的 BTCUSDT 多头 campaign。首次满足条件的调用作为 campaign 身份；同一连续真状态只允许一笔已成交母单（未成交可在后续原调用重试），首次假/缺失时结束身份。已持有的宏观仓位在状态持续真时占用唯一账户并阻挡出现的 SX60 新机会；状态首次转假/缺失的原调用按现有 SX60 分片计划退出，不在同次调用再入场。已有 SX60 仓位始终遵循原 SX60 退出，宏观状态不延长它。

宏观入场复用 SX60 的 5 分钟分片执行、1% 参与上限、费用、价差、滑点、资金、GAP、账户和冻结 20x 设置，单独使用现有波动率目标的 **3.6** 风险尺度；母单到固定原生止损的账户权益损失额另外封顶 **3%**。止损为调用前最后 **10** 个已完成 UTC 日线的最低价，止盈用现有 SX60 `entry_price*(entry_price/stop)^20` 几何规则；无有效止损几何或分钟成交证据则不新开仓。持仓只有原有交易所保护可在调用间动作；资金安全减仓、强平、止损、止盈和有证据的分钟竞争顺序全部走现有路径。不得因宏观仓位另开资金池、提升 SX60 的 6.0 风险尺度或虚构离线客户端退出。

## 顺序与门槛

保存每次调用的可见版本、最新/20 期前的观测日期/数值/可用时刻/缺失原因和原 ZIP/请求身份。先用同一价格、执行分钟和公共源码双跑开发 SX60 与覆盖增量，审计费用、资金费、资金约束、账本守恒和保护先后；宏观新成交/挤占原交易逐单配对。**仅开发连续账户同时 CAGR ≥150%、保守连续 MDD <50%、且资金/账本/保护证据合格，才运行同一冻结候选的 795 正式、原压力与 787 缺席。**历史已被反复查看，不能称为独立样本外；失败即否决，不能改参数补救。无法确认的分钟保护先后要先补官方原件，不能假设止损优先。

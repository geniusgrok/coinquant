# C2_lag7 连续账户：后验探索协议

## 身份与范围

- 本协议在账户计算前冻结。研究分支基线为 `2cf8368a94f09664d8d1edabe55ceedcea647880`；不修改 main、生产模型或执行权限。
- `C2_lag7` 是由先前滞后诊断正结果**后验**选定的唯一候选，状态为 `POST_HOC_EXPLORATORY / RETROSPECTIVE_PROXY`。它不能把原 48 小时主检验改写为通过，也不构成独立未见验证。
- 不搜索其他滞后、符号、窗口、模型、阈值、仓位或预算；首轮只跑 2020-01-01 至 2024-01-01 的连续开发账户。

## 已核验输入

- 关注度归档：`COINQUANT_ATTENTION_EVIDENCE_20260922.zip`，3,373,375 bytes，SHA-256 `ffd0195d2b7e47c8b9a7ee1bf7fae2f50a6fc3523eec96bca89d7e46157dfa56`；575 个登记成员均逐字节通过。复用其中 `lag7_predictions.json`、`lag7_fits.json`、`weeks.json` 和 `labels.json`，不重训旧月份或重做标签。
- payoff 归档：`COINQUANT_EXECUTABLE_PAYOFF_EVIDENCE_20260922.zip`，53,179,721 bytes，SHA-256 `0b46f6fccff4c68d7accc23ef126fbaefb6fdbb3143d3dfb6c74e5912c0c892f`；6,179 个登记成员均逐字节通过。
- 完整 Binance 开发输入来自已核验 native 归档；为与旧 B1 输入一致，补齐 2021-01-02、2021-03-13、2021-10-03、2022-02-24、2022-09-19 的分钟流。合并后 22 个分钟原件的路径与 SHA-256 均等于旧 B1 `inputs.json`。不重新下载行情。

## 账户映射

- 预测输入来自同一已保存的额外七日滞后文件：周结束后 48 小时再额外 7 天，即 `assumed_available_at = week_end + 9 天`；`available_at` 仍为 null，历史发布版本未证实。
- `C0` 是同月、已成熟 primary 训练行的保护后净收益均值；`C1` 是原五特征保护后净收益控制；`C2_lag7` 仅增加关注度偏离。`C0` 不是旧 terminal-label B0。
- 每个控制使用同一 `research.executable_payoff.account`，即原冻结稀疏调用、单一本金跨年、长/空仓、risk scale 3.6、既有波动 sizing、容量/资金限制、10 个完成日低点 SL、既有安全 TP、7 日后首次真实调用退出、同次退出不重入，以及实际费用、funding、保护和强平推进。预测幅度不改变仓位。
- `L3.6` 在当前同一共享回放引擎和同一 22 个分钟输入上重跑，配置为冻结的 `impulse_hold` 长仓、risk scale 3.6；不得以旧数字代替当前本轮对照。

## 计算与判断

- 保留冷启动、缺数、拒单和窗口尾部；只要保存的因果预测存在且账户仍在窗口内，就允许按原规则入场。标签未成熟不被倒灌为“当时没有信号”。期末仍按共享引擎估值。
- 每个账户保存预测/拟合/标签输入、代码源、副本、订单、权益、决策、独立现金账本核验和描述性归因。报告 CAGR、连续 MDD 包络、终值、逐年结果、费用、funding、强平/资不抵债、敞口、保证金、交易数、持有、未恢复期和利润集中度。
- 只有 `C2_lag7` 同时超过同口径 `C1` 与 `L3.6`、开发 MDD < 50%、无资不抵债且不依赖记账/路径错误时，才冻结并进入同一配置的完整窗口复核。否则不跑 2024+、不调参、不加预算；仅当归因证明一个不同于既有失败的可证伪结构原因时，才登记一次最小修订。

## 限制

Wikimedia 访问量仍是回溯代理，不能证明 2020--2023 当时的历史发布版本或可得时间。因此即使代理账户改善，也不能称正式 150% CAGR / <50% MDD 验收达标或启用生产执行。

# 五分钟执行风险预算：计算前冻结协议

2026-09-22；当前用户附件 PANCAKEQUANT_RETURN_FIRST_EXECUTION_RISK_FRONTIER_PROMPT_20260922.md 为有效授权。恢复研究362ef9f12e3683f93fe1ef97aea3e2c323b91ddf／main c886b7c63c6455bd7c933269e32cd35a6fb3e09a。尚未计算新预算账户。

## 有限问题和不变量

只研究内部risk_scale={3.6,4.8,6.0}。3.6为现有参照；最多四条新增开发主账户：4.8/6.0分别瞬时与五分钟。旧一分钟预算结果不能替代新组合。新授权替代旧“不得增加预算”范围限制，不追改旧协议或结果。当前研究、生产、测试源码目录Git树已与远端核对；原件48866772 bytes、SHA256 854fba74875a4e7bc07a9b709edf82b377d7157a01d8d73db42db44aa41095c1，2077个清单文件及CRC重新通过。未发现当前执行＋4.8/6.0等价结果。

本金CNY10000，无追加；开发2020-01-01至2024-01-01 exclusive，正式至2026-09-20 exclusive。目标CAGR>=150%、连续MDD<50%。固定原稀疏调用、L多头信号、目标公式、波动输入、保护、campaign、持有退出；交易所20x不变。五分钟窗口、首次延迟60秒、发布延迟60秒、1%参与率、成本、GAP、funding预留、维持保证金和名义上限均不变。每个独立账户按当时权益计算母单，母单冻结后只能保持或收缩。未知、未决、保护未确认、到期与重启阻止保持。

保留原共享Account/funded_target/ExecutionStudy。只开放研究驱动受限预算输入并记录完整身份；默认3.6不变。新增预算不得绕过旧引擎保护，旧容量取消诊断仍只允许3.6。真实新事件缺分钟时明确失败并保全，不从旧30个已成交小时推断新事件一定相同。只有实际缺口才补数，更新输入身份后只重验受影响结果。

## 压力、选择与全窗

主开发五分钟候选须CAGR高于既有五分钟3.6、MDD<50%、无已证实资金/因果/记账红线；符合者才各做一次原联合开发压力，最多两个：基础滑点及额外退出冲击系数同时翻倍、首次执行再延迟60秒，仍按原实现start+300秒截止。不得重解释端点、扫描成本或预算。
压力终值须高于五分钟3.6压力参照且不依赖漏记强平或无法支付资金。两个都满足时选压力CAGR较高者；相同选较低预算。不新增必须低于3.6回撤的门槛，压力尾险全部披露。
选择先保存提交；绑定选定预算、执行、开发/压力/账本结果、数据和源码哈希，再进入该唯一配置的一次全窗。禁止误用旧3.6继续条件。全窗改善后，对该候选和五分钟3.6新增一次同定义全窗压力配对，已有相同身份结果则复用。不看落选配置全窗、不插入中间值或提高上限。历史全窗重复使用，不称holdout。

## 风险、验证与保存

逐条独立连续现金账本；目标/预留/子单资金链路、默认3.6差异、输入和结果隔离作定向检查。已有53项检查按受影响路径复用，不默认全仓测试。观测余额/逐仓保证金/预留、敞口、止损及原跳空边界覆盖、强平距离、资金缩量拒绝和不确定事件；不能仅凭MDD放行。待成交资金在本回放为同步IOC无未决假设，不能冒充原生安全验证。
报告完整CAGR/MDD/终值、年收益/恢复、执行完成率、预算和执行交互、复利继承、全部盈亏事件、资金余量与成本。保留所有落选/失败原件。固定汇率、历史规则不全、分钟IOC/即时保护与线性退出冲击仍仅代理，不以现行文档倒灌历史事实。
源码和关键协议/选择/报告提交同研究分支；写前读ref，不覆盖并行变更。新原件文件式持久保存并回读长度、字节、SHA256、ZIP和清单；旧依赖固定版本引用。仅更新现有PROJECT_STATE.md及恢复索引。原生Git本环境DNS失败，使用授权GitHub连接器。当前技能目录无PonyTail可加载项，未伪称使用；沿用已加载适用工程方法。
main及生产默认配置不改，execute阻止保持，无实盘/Testnet写或安全设置授权。有限实验完成不等于正式总目标完成。

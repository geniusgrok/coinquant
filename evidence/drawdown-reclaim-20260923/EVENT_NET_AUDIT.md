# DC10 事件、成交与净账户的分层核对

使用已保存、逐字节核验的两份原件：795 个调用的 `DC10_SELECTION_FULL_CALL_LABELS.json`（Library `libfile_e9a6856d73d08191be34874f387c5531`，SHA-256 `0146c119104846cdc4b9ca3367989f45d58c42b4d726014d275d5c69398d892a`）和完整 468 调用开发账户 `COINQUANT_DC10_DEVELOPMENT_REJECT_20260923.zip`（Library `libfile_f323126832c08191b6b81999e03e435c`，SHA-256 `2ed9b042cd7000d9b3bece516ef8b47ffbf7c975d6f598faa7c3d29038442621`）。复现脚本 `event_net_audit.py` 输出 `EVENT_NET_AUDIT.json`。这是事后核查，不重新定义交易条件，也不把已看过的后段当作独立样本。

| 层次 | 核查结果 | 使用边界 |
|---|---|---|
| 可见价格条件 | 冻结 795 调用中，2025 年之前 168h 跌幅 ≥10% 的有 5 次 | 仅调用时可观察的条件；未含 DC10 四小时回收确认 |
| 独立价格事件 | 按 168h 窗口重叠连接为 3 个：3月1日 2 次、3月10日 1 次、11月20–22日 2 次 | 不能把 5 个相关调用算作 5 个独立交易或净收益 |
| 实际账户入场 | DC10 原冻结开发期 468 次调用，29 个母单创建且全部成交；全账户 29 个已关闭持仓分段 | 这是 2020–2023 的开发账户，**不是** 2025 价格事件的成交反事实 |
| 出场与成本后净结果 | 原冻结计划中 17 次 `regime_exit`、12 次止损见既有账户报告；29 分段 10 正、19 负；已发生钱包净额 −683.961354 USDT | 与 29 个母单首笔成交时间一一相符，逐年净额求和相符；不能拿未来七日开盘价代替退出 |
| 连续资金与复利 | 开发期成本后 CAGR −15.005879%，连续 MDD 61.116254%，期末 CNY 5,218.55 | 账户从原始冻结起点连续计时；本版本按原门槛拒绝 |

调用后 168h 的原始价格标签属于事后描述，未使用延迟成交、保护、资金费、手续费或人民币估值，不能从平均涨幅推断任何新策略的可执行净额。此核对同时指出下一项诊断需要先定义明确的入场与退出，再量化真实账户，而不是只凭标签或单笔毛价方向筛选。2025 的三组价格事件与先前报告“DC10 全窗有效调用 8 个、机会身份 2 个”分别采用不同判定，不能混合计数。

复现：`python3 evidence/drawdown-reclaim-20260923/event_net_audit.py /path/to/DC10_SELECTION_FULL_CALL_LABELS.json /path/to/COINQUANT_DC10_DEVELOPMENT_REJECT_20260923.zip`。正式最佳仍为 SX60 全窗成本后 CAGR 102.626211%、连续 MDD 39.885115%；生产 B36 与 `run --execute` 阻止保持。

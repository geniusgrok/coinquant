# 现货主动成交信息：本轮拒绝晋级

计算前协议提交：28878c5f57d03ee352e4f4c6e2fa7fe3b32d53be。
恢复研究HEAD：7ec83ab875486a29ca9369dd47cdb13008d230c8；main：c886b7c63c6455bd7c933269e32cd35a6fb3e09a。

## 实际结果

控制价格方向、波动、旧冲击方向与永续24h/7d成交不平衡后，本轮没有发现现货主动成交延续信息的可兑现增量。
24个评估月中两个现货特征的非负系数均为零；新旧模型预测与决策一致。训练残差方向导数的独立KKT核验也全部非正，说明这是冻结延续假设的拟合边界，不是新特征漏接。未翻转信号、改变窗口或增加预算。

| 2022–2023样本口径 | 样本数 | 控制模型成本后均值 | 加现货均值 | 配对增量 | 决策改变 |
|---|---:|---:|---:|---:|---:|
| 所有合格稀疏调用（标签重叠） | 230 | -0.255084% | -0.255084% | 0 | 0 |
| 时钟预选非重叠样本 | 80 | +0.452382% | +0.452382% | 0 | 0 |
| 非重叠2022 | 40 | -0.058751% | -0.058751% | 0 | 0 |
| 非重叠2023 | 40 | +0.963515% | +0.963515% | 0 | 0 |

这里是单次方向标签的算术均值，不是连续账户收益、CAGR或独立交易收益相加。
两个样本口径的差别说明抽样敏感性，不能只引用正的非重叠均值宣称控制模型成功。
同期多头参考均值：全部-0.549261%、非重叠-0.273872%；L长/空方向标签参考：全部+0.242918%、非重叠+0.190916%，均不代表L账户。
扩展模型在非重叠样本有67次非空方向，其中60次处于L方向为空时，但与控制模型完全相同，因此不是现货创造的新机会。
去掉最大增量事件、最大增量月份、最大扩展盈利事件或月份后，增量仍为零。月块区间[0,0]只说明两模型在这些样本上相同，绝不证明总体真实增量确定为零。

## 数据、因果性与范围

完整开发区形成428个有效标签，其中152个时钟非重叠标签；38个调用因特征缺数/异常而未形成有效特征（2020:20，2021:17，2022:0，2023:1）。全部遗漏时点单列保存，不按收益删除。原始现货8个异常小时被拒绝。没有运行账户，不能据此宣称缺数据期间账户已被正确保护。
恢复归档逐字节SHA256/长度一致，holding742、spot113、native1183个manifest成员核验。原件的官方checksum再次由加载器核验。
小时K线使用quote volume与taker-buy quote字段，完成后再留一小时延迟；微秒规范化有合成验证，本次2020–2023实测不含2025微秒数据。
官方字段、2025年微秒及可修订档案说明：https://github.com/binance/binance-public-data/blob/master/README.md 。历史归档不是当时可得版本；实时完成K线一小时发布延迟仍是代理假设。
按月训练仅使用结束时间早于月初7日的非重叠成熟标签，标准化仅用训练集；少于30样本空仓。标签退出是7日后的首次冻结调用，未来调用不进入特征或预测。
成本使用既有basis方向标签：双边7.5bp费率、冻结spread/slippage、实际资金费率及mark区间不利估值。历史经济规则仍未证明，信息检验不是资金/保护/强平验证。

## 验证与决策

新增4项定向检查通过：时序/缺数/单位、测试标签隔离、训练标签成熟、正向合成信号确实可以启用特征；既有basis4项通过。未运行全套测试。24月拟合KKT核验通过。
研究代码、样本、逐次预测、训练系数、数据身份、日志与失败结果全部保存；无生产策略或执行接口变动。
本轮冻结晋级条件失败，没有账户候选，因此没有新账户CAGR/MDD，也没有收益提升可报告。未读取2024+结果救援。
原L3.6代理67.0921% CAGR/32.0241% MDD保留，距离150%仍差82.9079个百分点；L6的82.1970%也未达标且保证金紧张。这里复用旧证据，不冒充本轮回测。
必要生产阻塞保持：历史档位/费用/完整人民币与USDT估值、原生部分成交与保护竞态、旧成交归属档案。默认执行阻止不撤，无真实/Testnet写入、无main合并。
只否定本次条件化线性延续机制，未证明所有现货信息或150%目标不可能。没有下一项实测支持的新alpha机制，不编造替代候选。下一必要工程仍可独立做原生成交归档恢复和保护竞态验证，但不能称为收益改进。
无会话外后台任务。项目总目标未完成。

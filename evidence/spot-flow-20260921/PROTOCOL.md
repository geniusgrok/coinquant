# Spot-flow protocol — frozen before new outcomes
Base: 7ec83ab875486a29ca9369dd47cdb13008d230c8. One hypothesis: persistent spot taker buying adds positive directional information conditional on price and perpetual flow. No reverse-sign rescue.
Inputs: existing identified BTCUSDT spot and perpetual hourly archives; reject malformed OHLC/time/volume/taker fields, missing/zero-volume hours. Normalize microsecond open/close timestamps before validation, never silently repair prices. One full hour publication lag; archive availability is a live-kline assumption, not contemporary archive publication proof. Revised archive vintages remain a limitation.
Features at invocation t: last usable open t-2h; trailing 24h/168h spot I=(2 sum buy_quote-sum quote)/sum quote. Controls: perpetual 24h/168h I, 24h/168h close returns, 168h hourly log-return SD, existing L impulse direction. No interaction/window/sign/threshold search.
Labels: gross perpetual open-to-open return from t to first frozen invocation u>=t+7d within development (2020-2023); u is label settlement only, never a feature. Cost-net side outcomes reuse basis_direction.direction_label adverse funding bounds, 7.5bp fee per side and spec friction. These are proxy executable labels, not protected account results.
Fit: monthly expanding, only clock-selected nonoverlap training observations with u < month_start-7d, at least 30; fit intercept plus standardized controls with ridge SSE+1*sum(beta^2), intercept unpenalized. Expanded same fit plus two spot features, coefficients constrained nonnegative for preregistered continuation; enumerate four active sets, minimize same objective. Means/SD only training. Cold-start flat. 2020-21 initial estimation, primary sequential evaluation 2022-23. No backfitting 2020.
Trade mapping: pred > 0.0037 => long; pred < -0.0037 => short; else flat. Identical fixed transaction-cost hurdle both models, no future funding in decision. Labels include actual adverse funding. Reference same-time long and L long/flat opportunity direction; L reference is a signal diagnostic, not its account.
Evaluate every eligible original invocation; primary inference uses clock-selected nonoverlap subset, defined before data validity. Report both; year means, paired net increment, changed decisions and new L-flat opportunities, block-bootstrap by calendar month (seed 20260921, 2000 draws), remove best incremental event/month and separately best expanded-profit event/month. Uncertainty descriptive, no confidence claim from small samples.
Advance only if primary mean net increment >0 in both 2022 and 2023 and remains positive after removing best incremental event and month; all-invocation increment must also be positive. Otherwise reject; no production integration, no 2024+ rescue, no parameter search. This is information-screen evidence, not CAGR.
If advanced: freeze a continuous candidate with causal monthly fitting/cold-start flat, same L3.6 sizing/shared Account and protection; no L prerequisite or blended accounts. Entry at sparse calls, retain native protection offline, review/close at first call after 7d or opposite/flat signal; no predicted future call. Compare control and extended at identical budget, costs, execution and exits; development first then one full-window historical recheck. Further one structural revision only if measured attribution warrants it.
Formal target unchanged: CNY10000/no additions, 2020-01-01 to 2026-09-20 exclusive, net CAGR>=150%, continuous MDD<50%, Binance perpetual fixed20x, original sparse schedule. Missing input forbids added risk; existing native protection remains. No live/testnet writes or main merge without full qualification.

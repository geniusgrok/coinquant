# UC4 四小时统一持仓：开发账户风险拒绝

## 本次实际新增的经济证据

实时 main 基线 `9678b140702b57cc6f87b3c56c6656de79590bda`；四小时20根高点突破入场、跌破前10根低点离场的统一状态与原资金、五分钟执行接通。原冻结开发468调用。首次入口漏接共享 `disposition`，在交易前失败，保留原始失败流；修复该接线错误未调整经济规则。后续两次运行均在 **2021-04-18 03:00 UTC** 的分钟代理同一小时遇到保护与强平边界同时跨越，原引擎拒绝凭空给出强平成交价。按保守包络，**在失败点以前**6.0内部风险尺度的最大连续回撤已达到 **76.154555%**，既有3.6尺度的单次配对控制达到 **57.588199%**；两者均已超过正式 `<50%` 风险门。因此两条流水都不是完整开发账户，不报告虚构CAGR或终值，不启动795全窗、联合压力或787缺席。

| 同一信号、同一输入及468调用 | 运行状态 | 已观察到的最大回撤下限 | 失败时持仓 BTC | 是否通过风险门 |
|---|---|---:|---:|---|
| UC4 6.0 | 2021-04-18分钟内止损/强平先后未定，账户停止 | 76.154555% | 2.026 | 否 |
| UC4 3.6既有预算诊断 | 同一时点停止 | 57.588199% | 0.456 | 否 |

6.0轨迹在停止前的止损为57,700、原10% GAP的强平边界约51,771.6494、该分钟最低mark为51,326.8331 USDT。先后顺序无法由一分钟的OHLC证明；既不能宣称交易所实际先强平，也不能事后选择止损先成交并续跑较好结果。3.6口径仍达到57.59%回撤，因此失败不只是6.0这一风险参数。数据资格、完整经济账户、真实历史成本和原生订单执行资格分别判断，后两项仍未闭合。

## 输入和保全

- 源码/原协议先于经济测量发布在研究分支 `research/unified-four-hour-channel-20260924`，提交 `994c90f7c3eb7159d072c7cc13ab51b33a2ff371`。修复唯一的缺失导入以后保留第一次失败和两条风险失败流水。风险配对的规则在首次测量前写入 `RISK_CONTROL_PROTOCOL.md`，使用项目已有3.6尺度，未扫描中间预算。
- 市场状态预检确定273个可能交易/退出调用小时，既有分钟覆盖210。新增50日所需的100份逐日原件中99份取得并验供应方CHECKSUM、ZIP与实际长度；官方 `2021-05-27` 逐日mark返回404，使用同供应方2021-05月度档及其CHECKSUM，具体请求和失败收据均保存。合并后所有273个可能小时的分钟OHLC、容量前置分钟均通过 `extend_minutes` 的小时重建验证；总细化小时361，输入身份 `f1f1c7c9ddb985d4b034c307267283c76e1e987cfd737d7b22bfa801d4e657f2`。
- 原旧大包及控制账户在 `evidence/renewal-risk-full-20260923/originals.json` 所列Library身份和Git主线索引；本轮新增50日原件完整保留在研究分支的 `evidence/unified-channel-20260924/minutes/`。本轮含失败原始账户流、决策、订单、日志、完整调用配置及源码快照的包见下列原件条目，来源和每个成员SHA-256存于包内 `MANIFEST.json`。
- 完整失败包 `COINQUANT_UC4_DEVELOPMENT_RISK_REJECT_20260924.zip`：**2,981,114字节**、SHA-256 `cad6b6b963a9b095f56268879d399d619e8c1477e829579f92100ad87bdc196d`、Library ID `libfile_3bfa4d5d98e48191a5d280fc2830e3f2`；回读与本地原件逐字节一致，包内36个成员的哈希及ZIP CRC核验通过。新官方分钟档另在Git研究分支，不把此2.9 MB失败账户包冒充完整市场输入。
- 定向单元检查覆盖了已完成四小时预热、状态与确认消费、退出及断档；加原活动、执行、资金相关检查共35项通过。它们不抵消经济风险失败。本轮没有实盘或Testnet下单，默认B36和`run --execute`阻止未变。

## 保留/拒绝与下一问题

正式已完成最佳 SX60 仍为795调用、成本后CAGR102.626211%、连续MDD39.885115%、终值CNY1,149,830.58；离150%目标仍差47.373789个百分点，按原年化口径终值目标约CNY4,717,248.98（约4.10倍）。UC4现有信号/风险配置拒绝；不因同一分钟成交先后未知而宣布已盈利或达标。下一机制应先解释如何在稀疏调用与原生保护下限制暴跌期间的账户损失，同时保留不同市场阶段的净收益机会；不能重复扫UC4预算或把历史2024以后当独立样本。先用原交易及权益原件定位最坏损失属于持仓方向、过高名义暴露还是保护跨越，并在新协议冻结前检查与M60/H60/HR60、旧通道方向策略是否等价。

# PR #38 后的因果归因和非对称短侧：事前协议

本文件在新账户收益输出前冻结。基线 main 为 `3f1700f7141741c63c5635ba6816f65e26eae2f1`。历史已被反复研究，本轮为探索，不能称独立样本外。原 `evidence/active-core-20260925/PROTOCOL.md` 不变。

## 共同合同

原 `research/spec.json`、`research/invocation_draws.json`、CNY 10,000、2020-01-01T00:00Z 至 2026-09-20T00:00Z（右端不含）、BTCUSDT USDT 永续、20x 交易所设置、原费用/延迟/汇率代理和 GAP、资金、数量、原生保护代理不变。开发 468、正式 795、缺席 787 次原调用。只在调用时作客户端决策；真实交易和 Testnet 均禁止，生产 B36/3.6 及 execute 阻止不变。

所有账户共享 `Account`、`BoundedEntry`、`PlannedExit` 和相同已发布市场输入。原 SX60 无新增母单风险比例限额，但继续受到原 `BoundedEntry.freeze` 损失预算、`funded_target` 与后续各子成交检查约束。母单按本账户当时余额、mark、信号重新决策，绝不复制另一账户的成交数量。

## 四格（仅新增 R1、R2）

| ID | PIR1 恢复子机会 | 原 SX60 母单新增风险限额 | 恢复子母单新增限额 | 作用 |
|---|---|---|---|---|
| S0 | 无 | 无 | — | 已有同输入对照；共享执行改动影响时仅补跑一次 |
| R1 | 无 | 当前权益 8% | — | 母单政策诊断，不参加正式选择 |
| R2 | 有 | 无 | 当前权益 4% | 新候选 |
| B60 | 有 | 当前权益 8% | 当前权益 4% | PR #38 旧结果；输入和源码适用时复用 |

R1 与 S0 的差别仅为母单新增 8%；R2 与 B60 的差别仅为该 8%。恢复机制导致的资金、仓位占用、消费/再武装及生命周期变化属于恢复因子，不冒充子交易的独立收益。计算 R1−S0、B60−R1、R2−S0、B60−R2 的连续账户终值、CAGR 和回撤，并报交互项 `(B60−R2)−(R1−S0)`；各项是账户比较，不拼接逐笔利润。复用旧 A36/A48 的母单请求、接受及绑定约束，解释相同结果，不新增 A 账户。

四格统一采用 PR #38 已准备的 B 市场小时并集和相同可用分钟输入；S0、R1 也载入 PIR1 市场小时用于**数据匹配**，不由此增加策略机会。短侧 C 的市场小时另按所有可能负向调用及其下一次调用取并集，不按账户收益筛选。正式前冻结各账户的 `input_identity` 与源文件摘要。

## 非对称多空（仅新增 C2、C4）

多头原样使用 S0 的 SX60：原信号、6.0 研究尺度、母单风险公式和退出，无 PIR1 子机会，无 PR #38 的新增 8%。短侧只在原调用时账户已确认空仓且当次没有可执行 SX60 多头时尝试。按原已发布延迟和 85 日预热取得 7/28/84 日归一化趋势；三个分量**全部严格小于零**才有效。连续有效日的首个已发布时刻为短侧稳定 campaign；无效/缺失使其重置。一次确认成交即消费，直到状态失效后重新出现才可再入场。同一调用完成退出后不得反手。

此处“可执行 SX60 多头”的调用时判定为：当次已发布机会为正、原波动目标非零、campaign 未消费、原入场价限制有效。最后的资金/精度/保护检查仍由同一账户执行；若该检查拒单，本调用不补开空头，不用成交后的信息重做选择。

短侧研究风险尺度为 3.6；C2/C4 的五分钟短母单保护止损总损失额外上限分别为当前权益 **2%/4%**。已有 GAP/资金/容量/最小量只能进一步减量。止损取调用前最后十根**已完成**日线的最高价，必须严格高于当次报价及 mark；决策价格 p 为当前调用报价经卖出方向 spread/slippage 后的参考成交价，止盈研究价 `p * (p / s)^20`，价格精度沿用现有 TICK，须满足 `0 < take < min(p,mark) <= max(p,mark) < stop`。母单冻结 stop/take，不随分片重算；不满足者拒绝。短侧状态失效时仅于下一原调用进入五分钟计划减仓，不凭盘后已知状态虚构中间退出。单向净仓、共同钱包、同一保证金及同一真实费用代理，不单独复利空头账户。

短侧实际成交价格、资金费逆向不利费率预留、保护先于下一切片、同分钟未知顺序、逐仓抵押及已确认保护、未知状态停止新增风险均须与多头同等级核验。保护触发/强平不能挑选有利分钟顺序；缺数据时补齐或保持未知。历史原生能力、动态费率/汇率、盘口与同步 IOC/保护仍是另行资格门。

## 运行及停止

在任何 R1/R2/C2/C4 收益输出前冻结此表、源码版本、输入身份及下述选择次序。先在原 468 次调用上分别运行全部四套。已发生 MDD≥50%、强平或原安全硬门失败可留有效前缀早停；数据缺失、未知结果或程序错误必须修复后继续原配置，不当经济否决。R1 只诊断；R2/C2/C4 必须同时满足开发 CAGR≥150%、完整连续 MDD<50%、账本/GAP/资金/保护及时序硬门，才按开发最终 CNY 终值选唯一正式者；同值低 MDD 优先，再按 R2、C2、C4。S0 已在正式失败，不再因开发数值重复晋级。

唯一候选从 CNY 10,000/原起点运行 795 次账户，校验开发前缀；正式 CAGR≥150%、MDD<50% 和硬门后，再运行原成本与延迟压力及 787 缺席，并逐项使用旧正式合同。正式失败不轮换备选，不按结果新增风险档位。完整保存决策、订单、连续权益、执行、数据哈希、审计及旧试验选择偏差；未完成原生执行资格保持 `NOT_QUALIFIED`。

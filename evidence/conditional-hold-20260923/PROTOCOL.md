# H60: computation-preceding protocol

Date: 2026-09-23. Baseline: geniusgrok/coinquant main `58dda2ee986ee85505d3df5e916f839f944185d2`. This is a post-hoc exploratory component test on reused history; no independent holdout is claimed. The accompanying user attachment defines the full contract.

E removes the fixed expiry unconditionally, and T also removes fixed take profit and installs a native trailing stop. Persistent impulse changes opportunity lifecycle. None conditionally retains only an expiring SX60 position using the existing M60 score while keeping SX60 entry, SL/TP, capital and five-minute execution. Verify any newer equivalent account before computing.

Single rule: keep `impulse_hold` entry and original opportunity identity, expiry, protection and SX60 scale 6.0. At a manual call, suppress a planned exit only if the filled opportunity's expiry is its sole invalidation, no intervening contrary impulse or other hard invalidation exists, capital/protection are valid, and the latest causally published 85-close 7/28/84 equal-weight M60 score is strictly positive. Lack of a complete published score denies extension without filtering entry. Once extended, any published nonpositive score since the preceding call, including a negative-to-positive offline transition, ends the old position at the next manual call. Do not reenter that same call or reactivate the expired entry opportunity. Native stops, takes, liquidation and funding continue between calls; risk reduction occurs only at actual calls. Do not add leverage, modify SL/TP, or append a new mother order.

Use a single shared Account/CapitalBudget/funded_target/PlannedExit path, original frozen 468/795/787 invocation sequences, CNY 10,000, full window 2020-01-01 through 2026-09-20 exclusive, continuous unrealized equity, costs/funding/capacity/FX and existing joint stress definitions. An unchanged SX60 control may be reused only with source and relevant input equivalence; otherwise rerun the affected control. Preserve all original data, orders, equity, execution and decisions including rejected/failed runs.

Proceed from the development continuous account to one full-window H60 run only if it changes a nontrivial action, CAGR > 0, continuous MDD < 50%, ledger and original GAP hold, and there is no confirmed insolvency, causality or capital error. Do not impose 150% or SX60 dominance in development. On full window, compare net final wealth, CAGR and continuous MDD to same-source SX60; only a wealth improvement with acceptable risk triggers the unchanged joint stress. Run frozen 787 absence calls if changed holdings make them material. Main integration preserves the 3.6 default and execute block, and cannot be called production qualification.

No period, weight, threshold, budget or filter search. A failed development risk prefix ends this economic candidate; neither later profit nor a hard-check flag overrides MDD. Record all assumptions and unmet native evidence explicitly.

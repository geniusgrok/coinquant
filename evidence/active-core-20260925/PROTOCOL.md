# Active core: preregistered economic experiment

Frozen on 2026-09-25 before any new A/B economic account was run. Parent `main`: `5ee74a7c0a0c25aaaa5dbc85ad96cba699df3965`. This document and the implementation are committed together before results are inspected. This is exploratory research on previously exposed history, not independent out-of-sample evidence.

## Contract and inputs

One existing shared `Account`, `funded_target`, `CapitalBudget`, `BoundedEntry`, `PlannedExit`, fee/funding/GAP, minute competition and reconciliation path; native 20x setting is not the budget. Binance BTCUSDT USDT perpetual. Start CNY 10,000 on 2020-01-01, right-open end 2026-09-20; development original 468 calls, formal 795, fixed absence 787, no external deposits. `research/spec.json` and `research/invocation_draws.json` unchanged. Price/mark/funding, official checked minute archives, 2026 quantity snapshot and original static conversion/fee/spread proxies have the same meaning as SX60. Existing B36/3.6 production and execute blocker untouched. All incremental minute requests come from **market-only possible call hours**, never from account profitability or selected fills. Old validated raw archives are reused; missing official bytes must be fetched or marked an objective input block. No invented within-minute order.

## Frozen mechanisms and configurations

The **only tuned economic parameter within each family is the existing effective exposure scale**. Stops, calendar expiry, daily horizons, coherence threshold and mother stop shares below are fixed structural rules and are not changed after results. A change to one is a different experiment and is outside this budget.

| ID | Family | Exposure scale | Shared structural rule | Five-minute total mother loss ceiling |
|---|---|---:|---|---:|
| A36 | A | 3.6 | Existing M60 completed 7/28/84-day normalized trend; all three components strictly positive to admit a long opportunity; same identity/consumption, 10-day completed-low stop, state invalidation at next original call, original safe distant native TP and sliced exits | 8% of current equity |
| A48 | A | 4.8 | Identical to A36 | 8% of current equity |
| B48 | B | 4.8 | Existing corrected PIR1 4-hour original impulse, first pullback, post-invalidation close confirmation, one child per parent, original/child campaign identity and consumption; no artificial rearming | Original mother 8%, child mother 4% of current equity |
| B60 | B | 6.0 | Identical to B48 | Original mother 8%, child mother 4% of current equity |

The 3.6/4.8/6.0 values reuse previously authorized execution scales; A avoids the prior failed 6.0 exact M60. The 8% stop ceiling permits multiple historical losses before a 50% drawdown and reserves uncommitted equity for extended trend holding, unlike a 20x leverage interpretation. B's reentry takes half the original parent's stop-risk allowance because a second confirmed opportunity after invalidation should not replace the lost parent's full risk budget. Each share caps the **aggregate** potential stop loss across the five slices using existing child fees and funding. Existing margin, volatility fraction, GAP reserve, position, volume and precision checks can only decrease accepted size; zero/under-minimum sizes do not round upward. The measured stop-risk budget is not a promise against gaps beyond the stop; GAP remains independent. No separate wallet or risk reset on child opportunities.

Pure `coinquant/active_core.py` makes the completed-state gate and per-mother policy auditable. Existing `Campaign`, `MultiscaleCampaign`, `Account`, `BoundedEntry`, `PlannedExit`, and causally published quote sequence own all other behavior. A held campaign that loses coherence is exited at the next original call using the already frozen planned exit; no client wakeup occurs between calls. B follows the corrected `disposition` ownership semantics, including holding the recorded parent while market child identity changes.

Family C (asymmetric long/short): the current bounded mother explicitly calls `funded_target(..., 1, ...)`, rejects `account.q < 0`, uses long-only quote/SL/TP geometry and capital anchoring; the shared planned exit/native protection readiness is not signed for shorts. A short diagnostic in the repo is not the original bounded five-minute account. No C configuration is frozen or run while this objective short execution integration block remains; A/B proceed. This is not an economic finding about short trends.

## Deterministic run, selection and stopping

Order: A36, A48, B48, B60. Run all executable configurations regardless of earlier losses. Reuse identical prepared inputs between scales if possible. A previously measured 6.0 M60 and 6.0 PIR1 are **not equivalent** to any row above (new state gate/stop allocation); their old losses remain valid context only. For each new development account, retain invocation, all hourly/hour and minute decisions, continuous equity, orders, execution events, source snapshots, input IDs, audit records and failed prefixes. On an explicit MDD ≥50%, confirmed liquidation or other original hard safety violation, stop that configuration at that prefix if the runner supports bounded termination; otherwise retain full trace. A missing minute, unknown competition, or implementation bug is **not** an economic rejection; resolve and resume the same row without changing any frozen rule. Do not introduce unregistered variants.

Qualify only if cost-after full 468-call development CAGR ≥150%, conservative continuous MDD <50%, shared ledger/schedule/causal/minute integrity, GAP, seven-day funding reserve and protection checks pass. Among **all** qualifying development rows, choose greatest final CNY equity; tie: lower MDD then fewer tuned parameters then ID order. Freeze one config and exact source/protocol/input identities before one fresh CNY 10,000 formal 795-call continuous replay. Its development prefix must be byte/identity equivalent to development account as applicable. Formal CAGR ≥150% and conservative MDD <50%, same mandatory safety checks, then run predeclared doubled cost + one-minute extra delay and fixed 787-call absence replay, no new parameter choice. If no development row qualifies, no formal candidate. If formal fails, no replacement. Only completed genuine market and account evidence can establish historical target; simulated static FX, fee tier, book, native order semantics and dynamic USDT/CNY gaps constrain true execution qualification. Seven-day finite reserve and 21-day same-rate deficiency must be reported separately.

Report each row's return, MDD, final funds, fees/funding, accepted opportunities, holding and exposure, margin, risk/protection incidents and year/concentration. Compare paired old SX60 under shared input assumptions and preserve revised PIR1 ownership results. Keep complete source, actual economic originals, audits, exact hashes, upload manifest and Git remote readback; do not overwrite failed runs.

# M60：计算前冻结协议

日期：2026-09-23。基线：geniusgrok/coinquant main d4256c4dee4c90c072923cb7dc45adcf545e708f；personal 连接。本提交先于本轮任何经济计算。用户附件 COINQUANT_MAIN_RETURN_FIRST_CORE_REDESIGN_PROMPT_20260923.md 为完整合同。本协议不报告新收益，不构成生产晋级。当前技能目录无 PonyTail；独立执行，不虚称调用。

## 唯一模型与身份

共享纯信号/状态入口拟为 coinquant/multiscale.py；专门研究入口 research/multiscale_replay.py；只对 research/persistent_hold_replay.py 做必要模型输入抽取，不复制账户或执行平台。原 SX60 默认行为保持，生产 3.6 默认与 execute 阻止不改。

已完成 UTC 日线 C，r=log(C_t/C_(t-1))；H={7,28,84}；u_h=log(C_t/C_(t-h))/sqrt(sum(r_j^2))，z_h=clip(u_h,-1,1)，score=(z_7+z_28+z_84)/3。全零收益对应分量0；缺数、非有限、非法价格停止新风险，不能当0制造反转。score>0 为多头，否则空仓，不按幅度调整风险，不搜周期/权重/门槛。必须85个连续完成收盘，不使用临时尺度权重。

日线结束+60000毫秒不晚于原调用T才可见，另存 bar_end / available_at / call_time / decision_complete / first_order。非正转正的首次 available_at 是 campaign 身份；完整预热首次正状态也用该时点，重建不重置。原调用空仓且未消费才可尝试入场；拒单不消费，确认部分成交消费。同 campaign 无日常调仓，原 campaign 已结束（包括离线负再正）则在调用计划退出；同次退出不重入。止损/止盈后同 campaign 不重入。没有七日趋势到期；七日只作资金规划。市场状态与成交归属分离，checkpoint 显式 M60 版本，不兼容解释旧 checkpoint。

## 仓位、保护与资金

内部风险尺度固定6.0；只多头，short_scale=0，volatility/one_campaign/fixed，原人工稀疏调用。共享 target_fraction 的20个日算术收益与七日资金尺度不变；对 M60 必须使用同一可见日线时点。共享 CapitalBudget/funded_target/sustain_position，10% GAP，固定20x交易所设置、容量、精度、原资金安全减仓均保留；缩量后不补回。

初始多头SL为当次可见过去10个完成UTC日最低价，floor到当前共享TICK。安全TP精确沿用非冲击初始化：price=本次入口quote*(1+slippage+spread/2)，TP=floor_step(price*(price/SL)^20,TICK)+TICK。几何不安全拒绝。入场后冻结该campaign/SL/TP，不放宽保护。

五分钟母单入场和SX60五分钟计划退出；主场景首次发单T+60秒，压力T+120秒，各自固定300秒窗口，参与率1%，容量取此前小时/60与已发布分钟量较小者。资金安全/紧急保护不等待分片。仅确认成交改变共享Account；未知结果阻止新风险，截止/余量使用原规则，不把ACK当成交。联合压力沿用当前实现：基础滑点及其额外退出冲击同时翻倍，不改spread、不拓宽窗口。

## 数据与控制

正常795次、缺席787次、开发468次；绑定 research/invocation_draws.json SHA256 3c409406a8bbe3bac31565a7795835b6c8a6051f1f5eb33a650672c5d75fd8cf，逐个时间戳验证。正式唯一市场采用 research/spec.json production_target 的 Binance BTCUSDT USDT永续，不采用顶层历史合约字段。

复用已核验SX60/执行边界/五分钟三份完整ZIP；SHA256分别d18021568ef10c5c091b1dc5488372a1d87e8b8bc0baadb5f0fe288df68956ab、fe46a9df34a4e9d80080aa25d79375f9f97517f2e73bfb45f110c24d6293d236、854fba74875a4e7bc07a9b709edf82b377d7157a01d8d73db42db44aa41095c1。数据单独恢复，不运行历史源码覆盖脚本。SX60共享函数/配置/调用的等价证据独立保存，不能把旧源码摘要当当前摘要。

现有预热为2019-12；仅补必要2019年本合约官方日线/校验文件（首先2019-10、11）。当时市场已存在不等于档案必然可得；无法取得可靠补充时按现有真实数据冷启动，空仓留在2020起账户，起点不移动。原小时、funding、标记价格与分钟原件尽量复用；只增量补新机会/退出/保护涉及的分钟及容量前置分钟。所有数据请求、缺数、校验和、输入身份保留，不能删除缺分钟事件或以小时假成交代替五分钟路径。

主比较为SX60，不以弱B36作收益提升依据。旧通道/L25慢均线/L20非线性记忆/持续冲击/方向变化不等价于上述归一化多尺度分数与campaign生命周期；读取原结果作去重，不重跑。无新证据不重做 flow/basis/B0B1/attention/lag7/T/预算/资金退出九账户。

## 连续账户与判定

CNY10000无追加，正式2020-01-01至2026-09-20 exclusive，开发至2024-01-01 exclusive，统一年化31556952000毫秒/年。费用、funding、转换、保证金、强平、容量与未实现权益走当前共享账本。固定FX=6.9762、初始转换10bp、费率/规则快照/盘口/原生撮合仍为原代理口径，NOT_QUALIFIED；不得称正式达标。

先开发单一连续账户。实现/账本/缺数错误保留首版后修复；经济MDD>=50%不允许后段掩盖。开发净收益正、无硬性失败、规则冻结则允许同一候选一次完整窗口，即使开发低于SX60；不要求开发先150%或超过SX60才查看全窗。完整覆盖795次，开发前缀一致，期末未平仓计入权益。历史2024+已多次研究，不称独立holdout。

完整主场景净收益超过SX60且MDD<50%才算本轮进步，随后同轮联合成本/延迟压力；不新增压力CAGR也必须150%的门槛。弱于SX60或风险失败则拒绝M60，本轮不扫参。若净改善而不足150%，仅有实测归因定位真实结构问题才最多一次事先记录的结构修订。实现纠错单独记录，不借纠错改经济条件。

独立现金账本、连续GAP审计、原生保护/截止/未知/重启/因果边界检查不可省。报告21日同率静态资金不足及实际最长持仓/有仓调用间隔，不用原787序列只删空仓的结果证明有仓离线安全。先复用静态诊断，必要时按事前有仓规则扩展，不事后择点。

## 交付

保存源码、测试、所有尝试与拒单、orders/equity/decisions、资金预算/减仓、父子单/退出/保护、输入校验与原件。报告SX60/M60开发与适用完整/压力CAGR、连续MDD、终值、年收益、最坏阶段/恢复、持仓/换手、费用/funding/冲击、实际名义/保证金权益/现金/GAP、强平未知缺数、目标终值及真实差距；归因仅描述，不冒充独立因果alpha。完整原件存Library并回读核验，不只留摘要。源码正常PR合并main，保持单一默认、只读、NOT_QUALIFIED，临时发布/采集工作流在合并前移除。

# Executable-payoff experiment — calculation-before-results freeze
Base a1a301fd00612109b46590215ee8bc4f1c1144a7; same branch. One long/flat candidate B1 vs same-structure B0. No spot features, no sign reversal, no budget/threshold/window search.
Engine: extend research.persistent_hold_replay.run with a bounded research-only entry/expiry policy and cached verified inputs. Both independent labels and both continuous accounts MUST call that engine, Account and funded_target. Existing path remains default; verify unchanged L3.6 development trace against saved originals.
Entry protection: existing non-impulse channel initialization: SL=floor(min low of last10 completed daily bars,TICK), TP=floor(entry*(entry/SL)^20,TICK)+TICK. This safety TP is inherited, not optimized. Reject unsafe geometry. Sizing: shared volatility target from20 completed daily returns *3.6, same current-instrument quantity proxy/capacity and funded_target reserves. Position fixed after fill. Fixed native SL/TP offline; close at first actual invocation at/after entry+7days. No signal reevaluation while held, no add/reduce/ratchet. No reentry at the same invocation as an exit; next distinct original invocation is a new opportunity, including after stops.
Each eligible invocation independent reference wallet CNY10000/6.9762*(1-.001) USDT, same entry and size. Terminal label disables SL/TP/early liquidation ONLY as a hypothetical endpoint training control, charges same proxy fees and adverse funding; may be economically infeasible, flag it, never claim executable. Protected label runs existing gap/SL/TP/liquidation-first events and minute refinement, stops funding after exit. Both normalize profit vs reference post-conversion wallet; no double conversion. They are overlapping scenarios, never add into account performance. Missing/unsafe/unfundable remain explicit status rows with no new risk; not silently removed from accounting.
Use all recovered development minute dates, identity/checksum/hour reconstruction; remaining hourly/minute ambiguity keeps original conservative liquidation-first treatment. If ambiguity drives label changes, do targeted path sensitivity before claims, not optional giant matrix.
Features same for B0/B1: 24h/168h completed-close returns,168h hourly log-return SD, current completed4h L impulse direction, causal stop distance/entry. No outcome/path flags in features. Complete bars only; same boundary-open execution proxy as existing account, not proven historical API publication/execution latency.
Labels end at first frozen call u>=t+7days. u may settle labels but is never a feature or known future decision clock. At month start train on clock-selected nonoverlap opportunities with u<month_start-7days; primary clock chosen before eligibility. At least30 eligible matured samples, otherwise flat. Expand monthly from2020; no backward application of2020-21 fit. Both use same rows and standardization; ridge SSE+1*sum(beta^2), intercept unpenalized. Only target differs. Enter when predicted net wallet return>0, never recost prediction or size by score.
Measure label sign/rank changes, exits, funding/fees, ambiguity and infeasible rows. Run independent continuous B0 and B1 development2020-2023 with real evolving equity and same sparse invocations. No filter on L3ATR. Compare B1-B0 and B1-L3.6 development89.5659701% CAGR, archived baseline drawdown and traces. Labels may not linearly scale capacity.
Freeze and full-window recheck B1 only if development CAGR exceeds BOTH B0 and L3.6 and MDD<50%, no insolvency, positive evidence not entirely ambiguous execution. Do not require MDD<32.02%. If no account improvement, reject; at most one separately registered structural revision only supported by measured attribution, no forced rescue.
Formal unchanged CNY10000/no additions,2020-01-01 through2026-09-20 exclusive,net CAGR>=150%,continuous MDD<50%,BTCUSDT perpetual,fixed20x,original sparse schedule. Proxy historical rules/FX/native-write gaps remain. No live/Testnet writes, no main merge or removal of execution block.
Save source, labels, paired paths, predictions/fits, continuous orders/equity/decisions, failures and input dependencies. Validation focused on shared-account equivalence, maturity/future perturbation, costs, gap/liquidation and unchanged baseline. No required full suite.

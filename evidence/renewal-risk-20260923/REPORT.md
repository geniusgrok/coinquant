# HR60 到期续持风险定仓实验报告

状态：唯一开发连续账户完成并通过附件规定的开发门槛；正式完整窗口因缺少可校验的 Binance 官方 1 分钟原件而未能完成。正式收益目标**未验证、未达标声明**。本研究不启用生产默认。

## 基线与旧实验等价性

- 以 `geniusgrok/coinquant` 实时 `main` `524c0d0fa16219e2dddf7bc1601182e064cc3e5a` 为起点；沿用 SX60 入场尺度 6.0、资金预算、GAP 抵押、费用/funding、分钟容量与退出冲击。HR60 的开发输入指纹与保存的 H60 开发账户完全一致：`e9bd485e3f7532956975485ea4eeb854376bd9e59d538e5f5c32c08a8bd9cf75`。
- 保存的最新 H60 开发账户为 468 次调用、20 个入场母意图、64 次许可续持；成本后 CAGR `183.080370%`、完整保守 MDD `54.278919%`、期末 `CNY 642,208.63`。只读原件复核发现历史风险暴露越过 50% 风险线；H60 未重跑。
- 旧 L8 在所有调用按当前权益动态重定仓，并允许 215 次增加与 160 次减少；其开发 CAGR `3.59%`、MDD `12.43%`、期末 `CNY 11,515.76`。旧 L13 会抬高止损并再投资；其开发 CAGR `18.34%`、MDD `72.68%`、期末 `CNY 19,610.36`。它们都没有冻结本次确认入场的 `r0=B0/E0`，与 HR60 的到期续持减仓约束不等价。旧网格未重跑。

## 唯一 HR60 开发账户

在 468 次冻结调用的同一连续账户上运行一次：

| 指标 | HR60 开发账户 |
|---|---:|
| 成本后 CAGR | 185.087191% |
| 完整保守 MDD | 37.148679% |
| 期末权益 | CNY 660,614.64 |
| H60 实际许可续持 | 64 |
| 续持风险检查 | 64 |
| 风险减仓 | 32 |
| 风险/资金执行后违规 | 0 |
| 入场风险基数 | 20 个确认入场母意图 |

减仓数量在许可调用后的 T+60 秒决策点，以该分钟开盘可见的价格、标记和已发布结算 funding 求出最大可行剩余数量，并在同一时点按既有 `exit_fill` 模型执行。完整审计确认 64 个待决检查均被决策或保护退出取消；32 次减仓均未增加仓位、未抬高原止损，GAP 保持、账户账本权益误差为 0、无强平、资金预算审计无入场违规。既有 SX60 母单确认子单照常保留；“只减仓”约束适用于 HR60 到期风险调整，不删除原入场执行成果。

该开发账户的成本后 CAGR 虽高于 150%，但开发回放只用于附件规定的入场门槛；它不能代替正式全窗，也不能证明收益目标达标。开发 MDD 低于 50% 且硬检查通过，因此已进入一次正式全窗准备。

## 正式全窗输入缺口

冻结正式序列为 795 次调用。第一次正式账户启动在 2020-10-24 18:00 UTC 因所需入场分钟没有进入验证覆盖而停止；该时刻的本地官方原件存在，但不在当前全窗分钟覆盖中。对需要分钟细化的 505 个潜在调用小时扩展原件后，核对发现仍缺少 378 个官方逐日分钟档案（`klines` 189 个、`markPriceKlines` 189 个），涉及 189 个日期；缺口清单在 `FULL_WINDOW_INPUT_GAP.json`。若按冻结 795 次调用作保守覆盖，还缺 874 个逐日档案，涉及 439 个日期。

没有用小时线替代、没有插值，也没有伪造全窗结果。由于现有附件没有补齐这些官方原件，本次没有可报告的 HR60 全窗 CAGR/MDD，也无法完成与同源 SX60 的正式净收益比较。按附件停止后续矩阵：不跑压力序列或 787 缺席序列。正式目标仍为全窗成本后 CAGR ≥150%、完整账户连续 MDD <50%；目前状态是**未验证**。

## 原件与验证

- 随报告交付的 `COINQUANT_HR60_EVIDENCE_20260923.zip` 内，`results/HR60-development/` 保存完整 468 调用账户原件、权益/订单/决策账本、执行事件、风险/资金审计、输入指纹和实际测量源码。
- 同一原件包内的 `results/HR60-development-failed-01/` 至 `failed-03/` 与 `results/HR60-full-window-failed-01/` 原样保留各次失败/被拒绝输出及其调用记录。
- `originals/` 保存用户附件和 H60 风险复核 ZIP 原件；`PROTOCOL.md` 保存本次冻结规则；`VALIDATION.json` 汇总可重现的硬检查和源码指纹。
- 针对性 40 项单元测试通过，相关 Python 文件编译通过。资金账本验证 35,064 个权益点、最大权益误差为 0；风险审计通过，原 GAP 保持，七日资金预算无违规。
- 仅合并研究代码和审计原件；不改 SX60 生产默认，不放开真实交易执行。主线合并不等于收益目标达标或实盘资格。

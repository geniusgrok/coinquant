# S：累积方向变化的完整持仓结构，计算前登记

L固定2.4/3.6/4.8/6.0范围开发已结束。冻结6.0完整回放为82.1970%CAGR、
41.5134%MDD包络，仍明显不足150%；不扩大预算范围。保留L3.6及L6.0原件。

假设：从运行低点累积上涨可以揭示持续需求，无需单根极端冲击。
用每日已完成行情的累积方向变化定义完整状态，捕获此前没有冲击入口的机会。
这不是新外部信息，也不预设具有alpha。

单一状态机S：使用上一日已知14日ATR，固定2ATR方向变化；初始中性，
从运行最低收盘向上2ATR转多，从最高收盘向下2ATR转空。
多状态追踪最高收盘，回落2ATR转空；空状态对称。触发日只使用当日已完成收盘。
每次状态反转创建唯一campaign；止损为确认前运行反向极值收盘，止盈沿用远端20R几何。
无固定到期，持有到方向失效后的下一稀疏调用或原生保护；一个campaign最多一次入场。
止损/止盈后不得在同一状态重复入场。仅多头，空状态目标为0。
两次调用之间只允许预置保护成交，日状态更新不是隐含交易。

与旧研究差别：L9/L17是固定20日高低突破锁存；L25是200日均值位置；
L26是固定通道中点连续目标；L16是固定20日OLS斜率的30日标签；
E仍必须单根3ATR冲击起始。S是路径依赖累积转向、浮动持续时间和极值失效，
既无固定通道长度，也不需要冲击事件，不是给L加过滤器。

参数固定，不搜索ATR倍数/周期；信号结构试验保持L3.6波动预算、
真实数量/成本/保证金代理引擎不变，库存持有不加仓。保护几何是结构的一部分，
不能把整体差异全归为方向信息。2020–2023稀疏完整账户直接检验新增机会成本后价值。
列出与L入口不重合交易净贡献、年度与集中度；若净收益不改善L3.6、MDD>=50%
或出现强平，拒绝晋级，不用2024+救援，不继续邻近阈值搜索。
只有通过时冻结完整窗口复核。否则继续共享状态工程及有证据的独立研究。

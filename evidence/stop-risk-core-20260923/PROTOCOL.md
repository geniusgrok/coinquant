# R10：SX60 整仓预计止损风险诊断（2026-09-23，事前冻结）

本次只检验资金约束与信号的关系，不声称 10% 是最佳份额，不把旧研究过的 2020—2023 视为独立样本外。正式目标和调用序列保持不变。

- 对照：已验证的 SX60，6.0 波动目标、仅多头、五分钟母单、原 sliced 退出；开发账户 468 次调用，同源 CNY 10,000。
- R10：除母单预算外行为与对照相同。母单冻结时，以当次 mark 计价的**完整账户权益**乘 0.10 与原预计止损预算取较小值。原目标数量仍由波动、资金、GAP、保证金、数量规则确定；这是两种不同的约束。
- 后续每个切片在原资金和容量约束之后，调用现有 `loss_at_stop` 测算**整个净持仓**，包括成交价、已有入场费、预计退出费、原止损价以及成交容量导致的退出冲击。必要时缩小增量，不为每个切片重新给 10%。原资金储备继续单独覆盖 funding；损失预算只累计母单窗口内实际已付 funding，不预扣同一远期储备两次。
- 母单发生时一次冻结预算；后续价格、权益变动、恢复和重试均沿用原母单记录，不从浮盈补足份额。仅原冻结的 468 次调用及既有五分钟执行时点允许主动加仓；无调用期间沿用原生保护模拟。持仓身份、退出、GAP、费用和成本不动。
- 输入：复用已核验 bounded 原件中的开发小时/旧 SX60 分钟，PIR1 原件中的开发分钟请求和扩展入场分钟，以及归属纠错包中补充的官方退出分钟。输入准备和 checksum/小时重建已在计算经济结果前完成，合并身份 `9b036837f4eb462d672524f9b032adc7a9a9ba85cce071b33b268ab658747645`，与上一轮配对 SX60 开发账户相同。使用原冻结 468 次调用。测量前记录源码哈希、配置、输入身份。候选连续回放保存现金账、权益流、订单流及失败轨迹。
- 开发硬门：成本后 CAGR ≥150%，完整连续 MDD 严格 <50%，账本、资金、保护和因果检查通过。R10 若失败，仅拒绝该诊断，不修改生产 B36，也不因此终止整个收益风险研究；比较实际净交易风险与后段覆盖，然后冻结新假设。
- 只有开发门通过且有明确优于对照的保留依据，才按同一协议运行正常 795 全窗；全窗失败则继续研究。数值、真实历史经济资格、原生执行资格分别判定，不授权任何实际交易。

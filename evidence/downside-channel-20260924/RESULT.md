# 下行通道空头：468 次开发调用的单次探针

2026-09-24；事前协议见 `PROTOCOL.md`，原始账户轨迹及输入身份见 `archive/MANIFEST.json`。本实验以 main `0ecd1a03058631d51b7b2d66aa97b11663ea1cf1` 为研究起点，未改生产默认或开放执行。

## 判定

冻结的 2020–2023 年开发期 468 次稀疏调用完整结束。新空头机制在单一成本后连续账户的**探针**中得到 CAGR **−34.747968%**、保守连续最大回撤 **91.036954%**、末值 **1,812.846045 元**（起始 10,000 元）。75 次入场、39 次止损、35 次通道失效退出，费用 176.044159 USDT，保守资金费支出 38.970868 USDT；35,064 个小时收盘权益点独立账本最大误差 0 USDT。它满足协议中“净收益非正且回撤至少 50%”的早停条件：**停止为这个方向扩展空头分片执行器，不让本候选进入正式全窗**。

这是在原账户算术上进行的较低成本诊断，尚未对称实现母单五分钟分片、持续资本与计划退出，也没有完整风险/执行审计。因此它**不是正式经济否决或合格策略**，上述 CAGR/MDD 不能与具备完整执行验证的 SX60 作严格配对优势检验。原 SX60 最佳正式 795 调用为 CAGR 102.626211%／MDD 39.885115%；正式目标 ≥150% 且 <50% 仍未达到。未运行本候选的 795 正式、联合压力或 787 缺席。

## 数据和因果边界

- 信号仅用上一根已经收盘的四小时柱及此前 20/10 根通道，按原时刻列表进行 468 次调用，128 次有方向信号；没有补造人工调用。
- 潜在保护 1,918 小时，要求对可能发生的交易价与 mark 保护区间给出官方一分钟证据；共请求细化 2,015 小时。探针读取包含旧研究原件和本轮补充数据，输入身份 SHA-256 为 `d8a28026ea9b994e43666cc14e09c6a82755593908240b6bd4ba1a72e9dfa3c1`，逐项索引在归档的 `probe-development-final.invocation.json`／`inputs.json`。
- 151 个新缺口日分四批取官方交易价和 mark ZIP/CHECKSUM，301 个请求通过官方 SHA-256、ZIP CRC、连续分钟和小时重建；2020-12-17 mark 的日档和月档均缺失或重复分钟，两个已校验的**官方原始档**保存于 `minutes/downside-minutes-0/rejected/`，回执如实标记 `rejected_original`，不能当作有效分钟档。
- 两个已下载 mark 分钟与官方小时 OHLC 不符（UTC 2020-09-25 07:00、2022-04-19 01:00）；`invocation.json` 保留差异和拒绝原因。在这些小时没有已持仓且需要细化的保护路径，已持仓路径仍失败关闭。不能宣称所有潜在小时都完成一分钟一致性验收，也不能外推到另一套策略。
- `result.json` 中通用回放器的 `limitations` 字段沿用既有文字（例如“305 days only”以及“full-position execution unverified”），不是本次官方日档完整性结论；以本报告、逐请求回执和 `invocation.json` 的精确记录为准。ZIP 内仅包含完成并验证的 `.csv.gz`，排除前次失败尝试残留的 `.partial`。

## 恢复与核验

`archive/reassemble.py /tmp/COINQUANT_DOWNSIDE_PROBE_20260924.zip` 依 manifest 顺序及逐片 SHA-256 重组并验证整体 SHA-256；预期 `23c1b95d497ef54c1b870291e156aa2f5f9724d6095be29aa94156cf054dfcba`。压缩包有完成的权益、订单、决策、结果、调用日志、逐项数据及测量时源码；内部 `FILE_MANIFEST.json` 逐文件校验。官方 ZIP/CHECKSUM 原件在同分支 `minutes/`；`missing_0.json` 至 `missing_3.json`、`RECEIPT.json` 给出完整请求及失败记录。不可用新的重跑结果覆盖原件。

下一步需先找有独立可检验机制依据的新候选，再按事前规则做完整开发账户双门，合格后才进行正式全窗和压力/缺席；不对本空头候选事后调参或把简化探针叫作生产资格。生产 B36、默认资金配置与 `run --execute` 阻止未变。

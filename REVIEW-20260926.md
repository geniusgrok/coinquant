# PR #43 逐项复核与修复（2026-09-26）

审查基线：main `7294efb09f3c359cdf8770e723bc21aa8035ce23`；原报告在 PR 头 `e7175cf92a38b0c5bb600dda11f45ffca29b2f59` 中完整保留。本文件更新为代码核验、故障复现和修复后的结论。

**原报告的大方向成立，但把交付缺口、明确的策略行为和代码缺陷混在了一起，并漏掉了保护归属等恢复风险。已确认的工程缺陷已修复；原生交易资格和当前会话经济验收仍未完成，不能因本 PR 合并而启用真实交易。**

## 原报告逐项判定

| 原结论 | 核验与处理 |
|---|---|
| 旧稀疏账户不能证明当前有限会话 | 正确。旧 SX60 约 102.63% CAGR、39.89% MDD；B36 约 74.97%、31.98%。均不是本次多空会话的重跑，收益未达 150%。MDD 是历史代理路径的保守极值包络，不能写成已观测的原生逐笔连续 MDD。 |
| CLI 禁止执行，生产验证未完成 | 事实正确。保留资格硬门；删除门控不能补足真实 IOC、部分成交、迟到成交和离线保护验证。本任务没有发真实或 Testnet 订单。 |
| 零成交 IOC 下一轮使用新身份重发 | 行为正确，属于明确入场政策，不是未知写入的幂等性漏洞。只有核对终态且零成交后才允许重评；未知写入仍恢复原身份。保留现有测试与行为；新经济评估必须覆盖轮询节奏与成本。 |
| 入场和原生止盈止损非原子 | 正确。保留成交核对、补保证金、原生保护及失败减仓的单一路径；真实无保护间隙、重复 close-all 接受行为无法用离线代码证明，仍是原生验收项。 |
| 当前规则很少修改保护价格 | 正确，但不是功能缺失。已有持仓按实际计划维护，价格改变时替换，机会失效时退出。本次修复替换中断后的恢复。 |
| 首次重建消费已存在机会 | 正确，属于冷启动政策。禁止把补取历史信号自动视作新委托；已有持久化检查点仍按原规则恢复。未因审查而发明新的追单规则，当前契约已写明。 |
| 三个月/满 1000 条就停止恢复 | 原代码确有缺陷，已修复。终态订单及核验成交持久归档；时间窗口满页以已核验 ID 边界续页，包括同一毫秒超过 1000 笔。交易所保留期仍是真实限制：连续归档支持旧入场，超过保留期的未观察缺口仍拒绝推断。 |
| 仅保存最后报告，没有会话账户历史 | 已补逐轮账户观察、未知缺口、不可变成交和原生收入流水；费用、正负资金费、已实现损益和转入转出按原生身份保存。运行期间才有实际观察；不会补造逐小时权益或声称已获得完整连续 MDD。 |
| 固定费用/MMR/汇率及不利侧资金费属于代理 | 正确，但资金费“不利侧才扣”是历史回放调用方政策，不是 linear_account 账户类的能力限制，账户类支持有符号资金费。历史原件不回写为真实历史费率；当前会话完整经济评估仍待做。 |
| 根规格仍是 Bybit BTCUSD | 已修复身份混淆。`research/spec.json` 是 Binance BTCUSDT、20×、有限会话的当前验收契约。旧规格逐字节保留为 `research/legacy/spec.json`，历史回放及哈希引用读取旧规格，冻结调用序列保持一致。 |

## 原报告遗漏及相邻风险修复

| 已复现问题 | 修复与验证 |
|---|---|
| 外部保护或触发价被改动的自有保护，可以满足快照聚合标志并被称为收尾成功 | 全部活动保护核对持久化所有权、方向、类型、执行标志和触发价；最终保护必须匹配当前计划的两张确定身份。外部保护/被改触发价均返回未知且不写单。 |
| 最近成交快照恰好 1000 条时，尚未进入完整恢复逻辑就被拒绝 | 最近窗口仅作账户游标，不冒充完整历史；满页不再阻断快照，完整性由归属恢复证明。 |
| 保护替换中断后，新旧单并存可提前通过聚合保护检查 | 恢复未完成的同一替换日志，先确认新保护、再逐个核验旧单退休；重启不生成新的替换身份。 |
| 时间查询显示满页，但 ID 查询截断/返回空也可能被信任 | 交叉核验时间页与 ID 页；ID 和时间必须单调前进，矛盾或重复分页返回未知，不推进覆盖记录。 |
| 审计接口故障可能阻止已确认到期退出 | 审计是新增风险的前置条件，减仓/保护维护先执行安全动作再审计；故障仍明确报告。 |
| 最后一次等待后断连，收尾失败可残留前一轮账户值 | 清除陈旧账户和模型观察，追加明确未知记录；不把旧权益再记作当前权益。 |

归档只有在成交数量、入场归属、实际仓位、二次账户观察及 campaign 时钟一致后推进。终态缓存只接受真实订单/子单终态，不把 ACK 缓存成成交证明。数据库沿用原账户锁与状态目录，没有新增账户、模型、后台进程或执行分支。

原生收入记录按 `(incomeType, tranId)` 去重，保留正负金额；重读重叠窗口检查不可变性，分页重复或缺失保留期覆盖返回未知。首次采集只声明最近观察起点，不追认全部过往历史。真实账户 MDD 还需要停机暴露期的估值证据；这个缺口没有用零现金流或权益插值掩盖。

## 验证

- Python 3.13.15：`python -m unittest discover -s tests -q`，362 项通过；覆盖原资金/风险、部分成交、撤单失败、状态恢复及生产/接口回放一致性。
- 新增 16 项 `tests/test_pr43.py` 风险回归和 1 项当前/历史规格身份测试。独立复核重复运行全部 16 项新增风险测试通过，修复后的复核未发现其他实质性缺陷。
- `python -m compileall -q coinquant research tests` 与 `git diff --check`；源码身份和结果见 `evidence/pr43-review-20260926/VERIFICATION.json`。
- 未重跑经济账户，未接触账户凭据、订单或账户设置。远端 CI 和合并状态以 PR #43 的实际记录为准。

接口边界核对来源：Binance 官方 [Account Trade List](https://developers.binance.com/docs/derivatives/usds-margined-futures/trade/rest-api/Account-Trade-List)、[Query Order](https://developers.binance.com/docs/derivatives/usds-margined-futures/trade/rest-api/Query-Order)、[Get Income History](https://developers.binance.com/docs/derivatives/usds-margined-futures/account/rest-api/Get-Income-History)。时间与 fromId 不组合；时间查询每窗不超过七天；历史保留期不被代码虚构延长。

## 仍需实际完成的交付条件

1. 指定账户环境中的原生只读核验，以及获得对应交易授权后的 IOC、部分成交、保护与替换、超时/迟到成交、退出后保护验证。现有环境缺账户配置和凭据，离线通过不等于这一门完成。
2. 当前有限会话时序的一份完整连续经济账户：冻结运行起止安排、使用当时可得的真实行情和成本资料，包含价差/滑点、手续费、正负资金费、分档保证金/强平、人民币兑换成本与停机持仓风险。目标区间和 ≥150% CAGR、<50% MDD 不变。

这些是尚未取得的实测证据与经济结果，不能作为已修复代码缺陷勾销。`run --execute` 继续在访问凭据和网络前阻止。

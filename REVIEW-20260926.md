# Coinquant 审查说明（2026-09-26）

基线：`main` `7294efb09f3c359cdf8770e723bc21aa8035ce23`（PR #42）。

这份说明只记录对照当前代码和已保存证据后仍成立的缺口。审查范围是作者给出的两个交付条件：历史经济验收，以及一条可以投入生产交易的 Binance BTCUSDT 执行路径。不评价更早研究阶段里已经否决的候选。

## 交付条件

两个条件都还没有完成。

历史目标仍是 2020-01-01 00:00 UTC 至 2026-09-20 00:00 UTC（右端不含），初始人民币 10,000 元、不追加，成本后 CAGR ≥ 150%，完整连续账户最大回撤 < 50%。已保存的正式账户没有达到收益门。这次工程更新没有重跑经济账户。`AGENTS.md` 写明：冻结的稀疏调用序列只保留作历史比较，不能证明新的会话时序。

生产写单也没有完成。`coinquant/cli.py` 在进入会话前拒绝 `run --execute`。公开行情、合约规则和四小时模型重建已经用 Binance 公共接口做过；账户只读、真实 IOC、部分成交后的保护、撤单与迟到成交、进程退出后保护是否触发，仍列在 `evidence/bounded-session-20260926/RESULT.md` 的未验证项里。

## 已有成绩不能代表当前会话

已公布的正式参照来自稀疏调用回放，不是 `coinquant/session.py` 的 `run()`。

| 记录 | 时钟 | 成本后 CAGR | 连续 MDD | 说明 |
|---|---|---:|---:|---|
| SX60 | 795 次冻结调用 | 102.63% | 39.89% | 研究尺度 6.0，正式收益门未过 |
| B36 | 同一类历史账户 | 74.97% | 31.98% | 旧的 3.6 记录，不是这次多空会话的重跑 |

150% 对应的终值大约是人民币 472 万。SX60 终值大约 115 万。保证金占权益在该账户上到过约 97%，实际敞口约 6.8 倍。这些数字属于当时那条回放，不能挪到现在的会话入口上。

当前生产模型仍是四小时 `impulse_hold`、风险尺度 3.6，`linear_preview.preview` 用 `side='both'`。这条多空接线没有一份 2020–2026 的成本后账户。此前在别的执行链上测过的正式空头，共用钱包内净额为负；那也不能当成这次会话做空的成绩。

`research/session_replay.py` 复用生产的 `session.run` 和 `Lifecycle`，核对离线请求磁带的顺序和参数。文件头写明它不是历史经济账户。因此「回测与实盘同一条执行代码」目前只覆盖脚本化的接口响应，没有覆盖完整窗口里的手续费、资金费、保证金分档、强平和汇率。

## 执行路径里已经写明、但还不能当验收的行为

### 当前命令不下单

`python -m coinquant run` 会按配置活一段有限时间：默认 300 秒，每 5 秒一轮，上限 24 小时，没有后台常驻。只读轮询会核对账户并给出模型预览。下单、撤单和保护替换在 `Lifecycle` 里，只有 `execute=True` 才发送。公开 CLI 到不了这一支。

### 打开执行后，未成交会按轮询重复发 IOC

入场是限价 IOC，不是留在盘上的挂单，也没有改价。`Lifecycle.enter` 每一轮用新的 `clientOrderId` 发一张单。零成交不会把该机会标成已消费，下一轮预览仍是 `enter`，于是再发一张。

`tests/test_session.py` 的 `test_zero_fill_retries_archive_terminal_entries` 把这写成预期：3 秒会话、零成交，归档 3 笔入场意图。会话长度是唯一的张数边界。默认 300 秒、5 秒一轮，同一未成交机会大约会发几十张；开到 24 小时会更多。历史回放是一次调用上按已完成分钟的成交量切几片。两种路径的成交次数、滑点和手续费不一样。现有经济原件没有用这个重试节奏重算。

未决撤单不会换身份：`test_cancel_timeout_does_not_get_new_identity_on_each_poll` 在撤单超时后只保留一次 DELETE。重复发单针对的是已经终态、且成交量为零的 IOC。

### 入场和保护不是原子的

`Lifecycle.protect_entry` 在成交核对之后补逐仓保证金，再挂 `closePosition` 止损和止盈。保护或补保证金失败时会尝试减仓，减仓本身若未知，异常继续抛出。这段持仓还没有交易所保护的时间，代码和 `evidence/bounded-session-20260926/RESULT.md` 都标成未实测。

能留在交易所上的是全仓条件单。持仓且机会身份一致时，`Lifecycle.maintain` 会在止损或止盈价格变化后替换保护，并先挂上新的一对再撤旧的。当前 `impulse_hold` 的止损取冲击 K 线中点，止盈在进场时按几何比定死，持仓期间价格通常不变，所以这条替换在现模型下很少被走到。机会到期或方向不再支持时，动作是退出，不是改保护价。

### 第一次重建会消费当时已经存在的机会

`linear_preview.advance` 在没有检查点时，用已完成的四小时 K 线把模型补到当前，然后把当时的活动机会记入 `consumed`。这样不会把补下来的历史行情当成一笔还没下的单。后果是：进程没开着的那段时间里已经出现、且启动时仍活动的冲击，这次运行不会再进。这之后新收盘产生的机会不受这一条影响。历史账户没有按真实的开关机空窗重放过。

## 恢复和账本

成交归属在 `coinquant/ownership.py`。有持仓时，入场意图的 `prepared_at` 必须落在大约最近三个日历月内，否则抛出 `fill history outside bounded recovery`。随后按 7 天一页拉取 `userTrades`，任一页达到 1000 条就视为可能截断并停止。两条都会拒绝继续改变风险，不能把超时的仓位接着管下去。冲击机会本身大约 7 天过期，但交易所上的持仓和保护可以留得更久；超过恢复窗口之后，下一次会话不能接管。

会话保存意图和最后一份状态报告，不写历史验收那种逐小时权益、费用和资金费账。以后有了真实成交，还不能用现有研究账本直接证明某次会话和某份历史账户是同一笔经济结果。

账户模式检查已经接在快照解码里：单向、单资产、逐仓、杠杆恰好 20 倍、不自动追加保证金、不能有其他品种仓位或其他结算资产。代码不会改这些设置。当前证据环境没有账户 UID 和密钥，所以这条检查还没有对真实账户跑过。

## 历史成本仍是代理

研究账与实盘预览用的成本来源不同。

- 研究账户的单边手续费、维持保证金、数量步长和价格步长写在 `coinquant/linear_account.py` 的常数里（手续费 7.5bp，维持保证金率 0.5%）。资金费只在对持仓不利时扣款，有利的一侧省略。人民币用 `research/spec.json` 里锁死的 6.9762。
- 实盘预览读当前的 `commissionRate`、杠杆分档和百档盘口，见 `coinquant/native_preview.py`。注释写明这些当前规则不能代替历史证据。

因此缺的是一份同时满足下面三项的账户：会话的决策和发单节奏、调用当时可见的行情，以及当时的费率、保证金分档、盘口、资金费和汇率。补当前盘口或当前手续费，不能回写成 2020 年以来的成绩。

`research/spec.json` 仍把验收身份要求为 Bybit `BTCUSD` 反向合约。Binance 线性写在旁边的 `production_target`。生产导入路径已经不再使用 `research/legacy` 里的 Bybit 代码；规格文件里的经济身份还没有改成这条会话。

## 建议先看的位置

| 问题 | 位置 |
|---|---|
| 执行硬门 | `coinquant/cli.py` |
| 会话循环与只读/写单分界 | `coinquant/session.py` |
| IOC 重复发单、保护非原子 | `coinquant/lifecycle.py` 的 `enter`、`protect_entry` |
| 零成交重试被测试固定下来 | `tests/test_session.py` `test_zero_fill_retries_archive_terminal_entries` |
| 首次启动消费当前机会 | `coinquant/linear_preview.py` `advance` |
| 三个月与 1000 条成交上限 | `coinquant/ownership.py` `reconcile` |
| 当前费率只用于预览 | `coinquant/native_preview.py` |
| 未做的交易所实测清单 | `evidence/bounded-session-20260926/RESULT.md` |
